/activity:
  $ref: 'activity/activity.yml'
/activities:
  $ref: 'activity/activities.yml'
/enrich/activities:
  $ref: 'enrich/activities.yml'
/feed/{feed_slug}/{user_id}/:
  $ref: 'feed/feed_slug__user_id.yml'
/enrich/feed/{feed_slug}/{user_id}:
  $ref: 'enrich/feed/feed_slug__user_id.yml'
/enrich/personalization/feed/:
  $ref: 'enrich/personalization/feed.yml'
/feed/{feed_slug}/{user_id}/{activity_or_foreign_id}:
  $ref: 'feed/feed_slug__user_id__activity_or_foreign_id.yml'
/feed/{feed_slug}/{user_id}/followers:
  $ref: 'feed/feed_slug__user_id/followers.yml'
/feed/{feed_slug}/{user_id}/following:
  $ref: 'feed/feed_slug__user_id/following.yml'
/feed/{feed_slug}/{user_id}/following/{target_feed_id}:
  $ref: 'feed/feed_slug__user_id/following__target.yml'
/feed_targets/{feed_slug}/{user_id}/activity_to_targets:
  $ref: 'feed_targets/feed_slug__user_id/activity_to_targets.yml'
/feed/add_to_many:
  $ref: 'feed/add_to_many.yml'
/follow_many:
  $ref: 'feed/follow_many.yml'
/reaction:
  $ref: 'reaction/reaction.yml'
/reaction/{id}:
  $ref: 'reaction/id.yml'
/reaction/{lookup_type}/{lookup_value}:
  $ref: 'reaction/lookup_type__lookup_value.yml '
/reaction/{lookup_type}/{lookup_value}/{kind}:
  $ref: 'reaction/lookup_type__lookup_value__kind.yml '
/og:
  $ref: 'og/og.yml'
/collections/{collection_name}:
  post:
/collections/{collection_name}/{id}:
  get:
  put:
  delete:
/collections:
  get:
  post:
  delete:
/files:
  post:
  delete:
/images:
  get:
  post:
  delete:
/user:
  post:
/user/{id}:
  get:
  put:
  delete: