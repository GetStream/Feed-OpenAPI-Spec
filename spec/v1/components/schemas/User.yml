EnrichedUser:
  type: object
  required:
    - id
  properties:
    id:
      type: string
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
    data:
      type: object
      description: any custom fields you have

